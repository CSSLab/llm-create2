# Copy this setting into server/.env alongside the existing FIREBASE_* admin credentials.
# This is a server-only key. Never prefix it with VITE_ or commit its value.
OPENROUTER_API_KEY=

# Set an explicit label for the audience pilot when launching it.
AUDIENCE_PILOT_VERSION=audience-poem-only-interpretation-pilot-v1
